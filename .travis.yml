language: go
go:
  - tip
sudo: required
services:
  - docker
before_install:
  - go get github.com/wadey/gocovmerge
  - go get github.com/mattn/goveralls
  - go get golang.org/x/tools/cmd/cover
script:
  - go test -v ./...
after_success:
  - scripts/generate_coverage.sh
  - goveralls -coverprofile=full_cov.out -service=travis-ci
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
#      other possible ldflags to decrease executable size at the cost of trace/debug: -s -w
      GOOS=linux go build -a --ldflags '-extldflags "-static"' . ;
      docker build -t cosminrentea/guble . ;
      docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD" ;
      docker push cosminrentea/guble ;
    fi
